import Vue from "vue";
import App from "./App.vue";
// import HeaderBar from "./HeaderBar.vue";

// Vue.component("cmp-header-bar", HeaderBar);
new Vue({
    el: "#app",
    render: (h) => h(App),
});
